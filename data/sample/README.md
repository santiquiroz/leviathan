# Sample data

`EURUSD_H1.csv` is **synthetic** (seeded random walk with trend regimes, MT5 export format). It exists so the quickstart runs out of the box — it is NOT real market data and results on it mean nothing. Export real data from MT5 or download from Dukascopy/Binance for actual research.
